/*
 * Code generated by Microsoft (R) AutoRest Code Generator 0.14.0.0
 * Changes may cause incorrect behavior and will be lost if the code is
 * regenerated.
 */

'use strict';

var models = require('./index');

var util = require('util');

/**
 * @class
 * Initializes a new instance of the ApplicationDescription class.
 * @constructor
 * @member {string} [name]
 * 
 * @member {string} [typeName]
 * 
 * @member {string} [typeVersion]
 * 
 * @member {array} [parameterList]
 * 
 */
function ApplicationDescription(parameters) {
  if (parameters !== null && parameters !== undefined) {
    if (parameters.name !== undefined) {
      this.name = parameters.name;
    }
    if (parameters.typeName !== undefined) {
      this.typeName = parameters.typeName;
    }
    if (parameters.typeVersion !== undefined) {
      this.typeVersion = parameters.typeVersion;
    }
    if (parameters.parameterList) {
      var tempParametersparameterList = [];
      parameters.parameterList.forEach(function(element) {
        if (element) {
          element = new models['ApplicationDescriptionParameterListItem'](element);
        }
        tempParametersparameterList.push(element);
      });
      this.parameterList = tempParametersparameterList;
    }
  }    
}


/**
 * Validate the payload against the ApplicationDescription schema
 *
 * @param {JSON} payload
 *
 */
ApplicationDescription.prototype.serialize = function () {
  var payload = {};
  if (this['name'] !== null && this['name'] !== undefined) {
    if (typeof this['name'].valueOf() !== 'string') {
      throw new Error('this[\'name\'] must be of type string.');
    }
    payload['Name'] = this['name'];
  }

  if (this['typeName'] !== null && this['typeName'] !== undefined) {
    if (typeof this['typeName'].valueOf() !== 'string') {
      throw new Error('this[\'typeName\'] must be of type string.');
    }
    payload['TypeName'] = this['typeName'];
  }

  if (this['typeVersion'] !== null && this['typeVersion'] !== undefined) {
    if (typeof this['typeVersion'].valueOf() !== 'string') {
      throw new Error('this[\'typeVersion\'] must be of type string.');
    }
    payload['TypeVersion'] = this['typeVersion'];
  }

  if (util.isArray(this['parameterList'])) {
    payload['ParameterList'] = [];
    for (var i = 0; i < this['parameterList'].length; i++) {
      if (this['parameterList'][i]) {
        payload['ParameterList'][i] = this['parameterList'][i].serialize();
      }
    }
  }

  return payload;
};

/**
 * Deserialize the instance to ApplicationDescription schema
 *
 * @param {JSON} instance
 *
 */
ApplicationDescription.prototype.deserialize = function (instance) {
  if (instance) {
    if (instance['Name'] !== undefined) {
      this['name'] = instance['Name'];
    }

    if (instance['TypeName'] !== undefined) {
      this['typeName'] = instance['TypeName'];
    }

    if (instance['TypeVersion'] !== undefined) {
      this['typeVersion'] = instance['TypeVersion'];
    }

    if (instance['ParameterList']) {
      var tempInstanceParameterList = [];
      instance['ParameterList'].forEach(function(element1) {
        if (element1) {
          element1 = new models['ApplicationDescriptionParameterListItem']().deserialize(element1);
        }
        tempInstanceParameterList.push(element1);
      });
      this['parameterList'] = tempInstanceParameterList;
    }
  }

  return this;
};

module.exports = ApplicationDescription;
