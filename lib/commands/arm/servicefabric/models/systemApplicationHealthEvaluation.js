/*
 * Code generated by Microsoft (R) AutoRest Code Generator 0.14.0.0
 * Changes may cause incorrect behavior and will be lost if the code is
 * regenerated.
 */

'use strict';

var models = require('./index');

var util = require('util');

/**
 * @class
 * Initializes a new instance of the SystemApplicationHealthEvaluation class.
 * @constructor
 * @member {array} [unhealthyEvaluations]
 * 
 */
function SystemApplicationHealthEvaluation(parameters) {
  SystemApplicationHealthEvaluation['super_'].call(this, parameters);
  if (parameters !== null && parameters !== undefined) {
    if (parameters.unhealthyEvaluations) {
      var tempParametersunhealthyEvaluations = [];
      parameters.unhealthyEvaluations.forEach(function(element) {
        if (element) {
          element = new models['UnhealthyEvaluation'](element);
        }
        tempParametersunhealthyEvaluations.push(element);
      });
      this.unhealthyEvaluations = tempParametersunhealthyEvaluations;
    }
  }    
}

util.inherits(SystemApplicationHealthEvaluation, models['HealthEvaluation']);

/**
 * Validate the payload against the SystemApplicationHealthEvaluation schema
 *
 * @param {JSON} payload
 *
 */
SystemApplicationHealthEvaluation.prototype.serialize = function () {
  var payload = SystemApplicationHealthEvaluation['super_'].prototype.serialize.call(this);
  if (util.isArray(this['unhealthyEvaluations'])) {
    payload['UnhealthyEvaluations'] = [];
    for (var i = 0; i < this['unhealthyEvaluations'].length; i++) {
      if (this['unhealthyEvaluations'][i]) {
        payload['UnhealthyEvaluations'][i] = this['unhealthyEvaluations'][i].serialize();
      }
    }
  }

  return payload;
};

/**
 * Deserialize the instance to SystemApplicationHealthEvaluation schema
 *
 * @param {JSON} instance
 *
 */
SystemApplicationHealthEvaluation.prototype.deserialize = function (instance) {
  SystemApplicationHealthEvaluation['super_'].prototype.deserialize.call(this, instance);
  if (instance) {
    if (instance['UnhealthyEvaluations']) {
      var tempInstanceUnhealthyEvaluations = [];
      instance['UnhealthyEvaluations'].forEach(function(element1) {
        if (element1) {
          element1 = new models['UnhealthyEvaluation']().deserialize(element1);
        }
        tempInstanceUnhealthyEvaluations.push(element1);
      });
      this['unhealthyEvaluations'] = tempInstanceUnhealthyEvaluations;
    }
  }

  return this;
};

module.exports = SystemApplicationHealthEvaluation;
