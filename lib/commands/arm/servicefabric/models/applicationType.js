/*
 * Code generated by Microsoft (R) AutoRest Code Generator 0.14.0.0
 * Changes may cause incorrect behavior and will be lost if the code is
 * regenerated.
 */

'use strict';

var models = require('./index');

var util = require('util');

/**
 * @class
 * Initializes a new instance of the ApplicationType class.
 * @constructor
 * @member {string} [name]
 * 
 * @member {string} [version]
 * 
 * @member {array} [defaultParameterList]
 * 
 */
function ApplicationType(parameters) {
  if (parameters !== null && parameters !== undefined) {
    if (parameters.name !== undefined) {
      this.name = parameters.name;
    }
    if (parameters.version !== undefined) {
      this.version = parameters.version;
    }
    if (parameters.defaultParameterList) {
      var tempParametersdefaultParameterList = [];
      parameters.defaultParameterList.forEach(function(element) {
        if (element) {
          element = new models['ApplicationTypeDefaultParameterListItem'](element);
        }
        tempParametersdefaultParameterList.push(element);
      });
      this.defaultParameterList = tempParametersdefaultParameterList;
    }
  }    
}


/**
 * Validate the payload against the ApplicationType schema
 *
 * @param {JSON} payload
 *
 */
ApplicationType.prototype.serialize = function () {
  var payload = {};
  if (this['name'] !== null && this['name'] !== undefined) {
    if (typeof this['name'].valueOf() !== 'string') {
      throw new Error('this[\'name\'] must be of type string.');
    }
    payload['Name'] = this['name'];
  }

  if (this['version'] !== null && this['version'] !== undefined) {
    if (typeof this['version'].valueOf() !== 'string') {
      throw new Error('this[\'version\'] must be of type string.');
    }
    payload['Version'] = this['version'];
  }

  if (util.isArray(this['defaultParameterList'])) {
    payload['DefaultParameterList'] = [];
    for (var i = 0; i < this['defaultParameterList'].length; i++) {
      if (this['defaultParameterList'][i]) {
        payload['DefaultParameterList'][i] = this['defaultParameterList'][i].serialize();
      }
    }
  }

  return payload;
};

/**
 * Deserialize the instance to ApplicationType schema
 *
 * @param {JSON} instance
 *
 */
ApplicationType.prototype.deserialize = function (instance) {
  if (instance) {
    if (instance['Name'] !== undefined) {
      this['name'] = instance['Name'];
    }

    if (instance['Version'] !== undefined) {
      this['version'] = instance['Version'];
    }

    if (instance['DefaultParameterList']) {
      var tempInstanceDefaultParameterList = [];
      instance['DefaultParameterList'].forEach(function(element1) {
        if (element1) {
          element1 = new models['ApplicationTypeDefaultParameterListItem']().deserialize(element1);
        }
        tempInstanceDefaultParameterList.push(element1);
      });
      this['defaultParameterList'] = tempInstanceDefaultParameterList;
    }
  }

  return this;
};

module.exports = ApplicationType;
