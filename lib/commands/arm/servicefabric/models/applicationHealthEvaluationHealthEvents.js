/*
 * Code generated by Microsoft (R) AutoRest Code Generator 0.14.0.0
 * Changes may cause incorrect behavior and will be lost if the code is
 * regenerated.
 */

'use strict';

var models = require('./index');

var util = require('util');

/**
 * @class
 * Initializes a new instance of the ApplicationHealthEvaluationHealthEvents class.
 * @constructor
 * @member {number} [kind]
 * 
 * @member {string} [description]
 * 
 * @member {number} [aggregatedHealthState]
 * 
 * @member {string} [applicationName]
 * 
 * @member {array} [unhealthyEvaluations]
 * 
 */
function ApplicationHealthEvaluationHealthEvents(parameters) {
  if (parameters !== null && parameters !== undefined) {
    if (parameters.kind !== undefined) {
      this.kind = parameters.kind;
    }
    if (parameters.description !== undefined) {
      this.description = parameters.description;
    }
    if (parameters.aggregatedHealthState !== undefined) {
      this.aggregatedHealthState = parameters.aggregatedHealthState;
    }
    if (parameters.applicationName !== undefined) {
      this.applicationName = parameters.applicationName;
    }
    if (parameters.unhealthyEvaluations) {
      var tempParametersunhealthyEvaluations = [];
      parameters.unhealthyEvaluations.forEach(function(element) {
        if (element) {
          element = new models['ServiceTypeUnhealthyEvaluation'](element);
        }
        tempParametersunhealthyEvaluations.push(element);
      });
      this.unhealthyEvaluations = tempParametersunhealthyEvaluations;
    }
  }    
}


/**
 * Validate the payload against the ApplicationHealthEvaluationHealthEvents schema
 *
 * @param {JSON} payload
 *
 */
ApplicationHealthEvaluationHealthEvents.prototype.serialize = function () {
  var payload = {};
  if (this['kind'] !== null && this['kind'] !== undefined) {
    if (typeof this['kind'] !== 'number') {
      throw new Error('this[\'kind\'] must be of type number.');
    }
    payload['Kind'] = this['kind'];
  }

  if (this['description'] !== null && this['description'] !== undefined) {
    if (typeof this['description'].valueOf() !== 'string') {
      throw new Error('this[\'description\'] must be of type string.');
    }
    payload['Description'] = this['description'];
  }

  if (this['aggregatedHealthState'] !== null && this['aggregatedHealthState'] !== undefined) {
    if (typeof this['aggregatedHealthState'] !== 'number') {
      throw new Error('this[\'aggregatedHealthState\'] must be of type number.');
    }
    payload['AggregatedHealthState'] = this['aggregatedHealthState'];
  }

  if (this['applicationName'] !== null && this['applicationName'] !== undefined) {
    if (typeof this['applicationName'].valueOf() !== 'string') {
      throw new Error('this[\'applicationName\'] must be of type string.');
    }
    payload['ApplicationName'] = this['applicationName'];
  }

  if (util.isArray(this['unhealthyEvaluations'])) {
    payload['UnhealthyEvaluations'] = [];
    for (var i = 0; i < this['unhealthyEvaluations'].length; i++) {
      if (this['unhealthyEvaluations'][i]) {
        payload['UnhealthyEvaluations'][i] = this['unhealthyEvaluations'][i].serialize();
      }
    }
  }

  return payload;
};

/**
 * Deserialize the instance to ApplicationHealthEvaluationHealthEvents schema
 *
 * @param {JSON} instance
 *
 */
ApplicationHealthEvaluationHealthEvents.prototype.deserialize = function (instance) {
  if (instance) {
    if (instance['Kind'] !== undefined) {
      this['kind'] = instance['Kind'];
    }

    if (instance['Description'] !== undefined) {
      this['description'] = instance['Description'];
    }

    if (instance['AggregatedHealthState'] !== undefined) {
      this['aggregatedHealthState'] = instance['AggregatedHealthState'];
    }

    if (instance['ApplicationName'] !== undefined) {
      this['applicationName'] = instance['ApplicationName'];
    }

    if (instance['UnhealthyEvaluations']) {
      var tempInstanceUnhealthyEvaluations = [];
      instance['UnhealthyEvaluations'].forEach(function(element1) {
        if (element1) {
          element1 = new models['ServiceTypeUnhealthyEvaluation']().deserialize(element1);
        }
        tempInstanceUnhealthyEvaluations.push(element1);
      });
      this['unhealthyEvaluations'] = tempInstanceUnhealthyEvaluations;
    }
  }

  return this;
};

module.exports = ApplicationHealthEvaluationHealthEvents;
