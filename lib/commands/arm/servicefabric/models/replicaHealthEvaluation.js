/*
 * Code generated by Microsoft (R) AutoRest Code Generator 0.14.0.0
 * Changes may cause incorrect behavior and will be lost if the code is
 * regenerated.
 */

'use strict';

var models = require('./index');

var util = require('util');

/**
 * @class
 * Initializes a new instance of the ReplicaHealthEvaluation class.
 * @constructor
 * @member {string} [partitionId]
 * 
 * @member {string} [replicaOrInstanceId]
 * 
 * @member {array} [unhealthyEvaluations]
 * 
 */
function ReplicaHealthEvaluation(parameters) {
  ReplicaHealthEvaluation['super_'].call(this, parameters);
  if (parameters !== null && parameters !== undefined) {
    if (parameters.partitionId !== undefined) {
      this.partitionId = parameters.partitionId;
    }
    if (parameters.replicaOrInstanceId !== undefined) {
      this.replicaOrInstanceId = parameters.replicaOrInstanceId;
    }
    if (parameters.unhealthyEvaluations) {
      var tempParametersunhealthyEvaluations = [];
      parameters.unhealthyEvaluations.forEach(function(element) {
        if (element) {
          element = new models['UnhealthyEvaluation'](element);
        }
        tempParametersunhealthyEvaluations.push(element);
      });
      this.unhealthyEvaluations = tempParametersunhealthyEvaluations;
    }
  }    
}

util.inherits(ReplicaHealthEvaluation, models['HealthEvaluation']);

/**
 * Validate the payload against the ReplicaHealthEvaluation schema
 *
 * @param {JSON} payload
 *
 */
ReplicaHealthEvaluation.prototype.serialize = function () {
  var payload = ReplicaHealthEvaluation['super_'].prototype.serialize.call(this);
  if (this['partitionId'] !== null && this['partitionId'] !== undefined) {
    if (typeof this['partitionId'].valueOf() !== 'string') {
      throw new Error('this[\'partitionId\'] must be of type string.');
    }
    payload['PartitionId'] = this['partitionId'];
  }

  if (this['replicaOrInstanceId'] !== null && this['replicaOrInstanceId'] !== undefined) {
    if (typeof this['replicaOrInstanceId'].valueOf() !== 'string') {
      throw new Error('this[\'replicaOrInstanceId\'] must be of type string.');
    }
    payload['ReplicaOrInstanceId'] = this['replicaOrInstanceId'];
  }

  if (util.isArray(this['unhealthyEvaluations'])) {
    payload['UnhealthyEvaluations'] = [];
    for (var i = 0; i < this['unhealthyEvaluations'].length; i++) {
      if (this['unhealthyEvaluations'][i]) {
        payload['UnhealthyEvaluations'][i] = this['unhealthyEvaluations'][i].serialize();
      }
    }
  }

  return payload;
};

/**
 * Deserialize the instance to ReplicaHealthEvaluation schema
 *
 * @param {JSON} instance
 *
 */
ReplicaHealthEvaluation.prototype.deserialize = function (instance) {
  ReplicaHealthEvaluation['super_'].prototype.deserialize.call(this, instance);
  if (instance) {
    if (instance['PartitionId'] !== undefined) {
      this['partitionId'] = instance['PartitionId'];
    }

    if (instance['ReplicaOrInstanceId'] !== undefined) {
      this['replicaOrInstanceId'] = instance['ReplicaOrInstanceId'];
    }

    if (instance['UnhealthyEvaluations']) {
      var tempInstanceUnhealthyEvaluations = [];
      instance['UnhealthyEvaluations'].forEach(function(element1) {
        if (element1) {
          element1 = new models['UnhealthyEvaluation']().deserialize(element1);
        }
        tempInstanceUnhealthyEvaluations.push(element1);
      });
      this['unhealthyEvaluations'] = tempInstanceUnhealthyEvaluations;
    }
  }

  return this;
};

module.exports = ReplicaHealthEvaluation;
