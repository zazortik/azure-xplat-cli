/*
 * Code generated by Microsoft (R) AutoRest Code Generator 0.14.0.0
 * Changes may cause incorrect behavior and will be lost if the code is
 * regenerated.
 */

'use strict';

var models = require('./index');

var util = require('util');

/**
 * @class
 * Initializes a new instance of the ServicesHealthEvaluation class.
 * @constructor
 * @member {string} [serviceTypeName]
 * 
 * @member {array} [unhealthyEvaluations]
 * 
 * @member {number} [totalCount]
 * 
 * @member {number} [maxPercentUnhealthyServices]
 * 
 */
function ServicesHealthEvaluation(parameters) {
  ServicesHealthEvaluation['super_'].call(this, parameters);
  if (parameters !== null && parameters !== undefined) {
    if (parameters.serviceTypeName !== undefined) {
      this.serviceTypeName = parameters.serviceTypeName;
    }
    if (parameters.unhealthyEvaluations) {
      var tempParametersunhealthyEvaluations = [];
      parameters.unhealthyEvaluations.forEach(function(element) {
        if (element) {
          element = new models['UnhealthyEvaluation'](element);
        }
        tempParametersunhealthyEvaluations.push(element);
      });
      this.unhealthyEvaluations = tempParametersunhealthyEvaluations;
    }
    if (parameters.totalCount !== undefined) {
      this.totalCount = parameters.totalCount;
    }
    if (parameters.maxPercentUnhealthyServices !== undefined) {
      this.maxPercentUnhealthyServices = parameters.maxPercentUnhealthyServices;
    }
  }    
}

util.inherits(ServicesHealthEvaluation, models['HealthEvaluation']);

/**
 * Validate the payload against the ServicesHealthEvaluation schema
 *
 * @param {JSON} payload
 *
 */
ServicesHealthEvaluation.prototype.serialize = function () {
  var payload = ServicesHealthEvaluation['super_'].prototype.serialize.call(this);
  if (this['serviceTypeName'] !== null && this['serviceTypeName'] !== undefined) {
    if (typeof this['serviceTypeName'].valueOf() !== 'string') {
      throw new Error('this[\'serviceTypeName\'] must be of type string.');
    }
    payload['ServiceTypeName'] = this['serviceTypeName'];
  }

  if (util.isArray(this['unhealthyEvaluations'])) {
    payload['UnhealthyEvaluations'] = [];
    for (var i = 0; i < this['unhealthyEvaluations'].length; i++) {
      if (this['unhealthyEvaluations'][i]) {
        payload['UnhealthyEvaluations'][i] = this['unhealthyEvaluations'][i].serialize();
      }
    }
  }

  if (this['totalCount'] !== null && this['totalCount'] !== undefined) {
    if (typeof this['totalCount'] !== 'number') {
      throw new Error('this[\'totalCount\'] must be of type number.');
    }
    payload['TotalCount'] = this['totalCount'];
  }

  if (this['maxPercentUnhealthyServices'] !== null && this['maxPercentUnhealthyServices'] !== undefined) {
    if (typeof this['maxPercentUnhealthyServices'] !== 'number') {
      throw new Error('this[\'maxPercentUnhealthyServices\'] must be of type number.');
    }
    payload['MaxPercentUnhealthyServices'] = this['maxPercentUnhealthyServices'];
  }

  return payload;
};

/**
 * Deserialize the instance to ServicesHealthEvaluation schema
 *
 * @param {JSON} instance
 *
 */
ServicesHealthEvaluation.prototype.deserialize = function (instance) {
  ServicesHealthEvaluation['super_'].prototype.deserialize.call(this, instance);
  if (instance) {
    if (instance['ServiceTypeName'] !== undefined) {
      this['serviceTypeName'] = instance['ServiceTypeName'];
    }

    if (instance['UnhealthyEvaluations']) {
      var tempInstanceUnhealthyEvaluations = [];
      instance['UnhealthyEvaluations'].forEach(function(element1) {
        if (element1) {
          element1 = new models['UnhealthyEvaluation']().deserialize(element1);
        }
        tempInstanceUnhealthyEvaluations.push(element1);
      });
      this['unhealthyEvaluations'] = tempInstanceUnhealthyEvaluations;
    }

    if (instance['TotalCount'] !== undefined) {
      this['totalCount'] = instance['TotalCount'];
    }

    if (instance['MaxPercentUnhealthyServices'] !== undefined) {
      this['maxPercentUnhealthyServices'] = instance['MaxPercentUnhealthyServices'];
    }
  }

  return this;
};

module.exports = ServicesHealthEvaluation;
